# 客服記錄系統前端架構說明

## 入口檔

- `客服記錄系統.html`
  - 保留頁面結構、畫面 DOM、樣式與 script 載入順序。
  - 不再承擔主要商業邏輯。

## 模組分工

- `客服記錄系統.config.js`
  - 共用常數與 UI 文案。
- `客服記錄系統.core.js`
  - 日期欄位正規化、資料映射、共用狀態、共用小工具與案件規則。
- `客服記錄系統.data.js`
  - Supabase REST 呼叫、案件 CRUD、資料載入、操作紀錄載入。
- `客服記錄系統.form.js`
  - 新增、編輯、快速輸入、表單存檔流程。
- `客服記錄系統.list.js`
  - 篩選、搜尋、列表渲染、明細顯示輔助。
- `客服記錄系統.analytics.js`
  - 分析報表畫面與統計渲染。
- `客服記錄系統.app.js`
  - 啟動流程、tab 切換、分類下拉、modal 背景關閉。
- `客服記錄系統.status.js`
  - 狀態快捷選單與快速更新。
- `客服記錄系統.import.js`
  - Excel 匯入、欄位映射、重複判斷、預覽與寫入。
- `客服記錄系統.todo.js`
  - 每日提醒列與提醒 chips。
- `客服記錄系統.batch.js`
  - 批次選取與批次更新。
- `客服記錄系統.actions.js`
  - 發票完成、刪除案件、複製結案通知。
- `客服記錄系統.report-tools.js`
  - 案件流水號、Excel 匯出、報表週期與日期篩選工具。

## 建議載入順序

1. `config`
2. `core`
3. `data`
4. `form`
5. `list`
6. `analytics`
7. `app`
8. `status`
9. `import`
10. `todo`
11. `batch`
12. `actions`
13. `report-tools`

## 維護原則

- 共用常數優先放 `config.js`
- 共用資料規則與日期轉換優先放 `core.js`
- API 與 Supabase 寫入只放 `data.js`
- 互動流程盡量依功能分檔，不再回塞進 HTML
- 若新增新區塊，先判斷它屬於：
  - 資料層
  - 表單層
  - 列表層
  - 分析層
  - 跨頁互動層

## 目前待優化但不急

- 將更多重複 toast 文案集中到 `config.js`
- 將部分 inline style 逐步轉成 CSS class
- 後續可再抽出 `notifications.js` 或 `constants.js`
